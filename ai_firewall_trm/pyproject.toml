[project]
name = "ai-firewall-trm"
version = "0.1.0"
description = "Lightweight AI firewall pipeline with TRM/CTM-friendly adapters."
requires-python = ">=3.10"

[tool.setuptools]
package-dir = {"" = "src"}

[tool.setuptools.packages.find]
where = ["src"]
